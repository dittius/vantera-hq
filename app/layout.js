export const metadata = {
  title: "VANTERA HQ",
  description: "Autonomous AI-native holding company headquarters",
};

export default function RootLayout({ children }) {
  return <html lang="en"><body style={{margin: 0, background: "#090b0f"}}>{children}</body></html>;
}
